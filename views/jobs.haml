%h2
  Jobs

- if jobs.empty?
  %p
    The list of jobs is empty.
- else
  %table.compact
    %colgroup
      %col{style: 'width: 10em'}
      %col{style: ''}
      %col{style: 'width: 4em'}
    %thead
      %tr
        %th
          Id
        %th
          Log
        %th
          Started
    %tbody
      - jobs.each do |j|
        %tr
          %td
            %a{href: iri.cut('/job').add(id: j[:id])}= j[:id][0..7]
            %a{href: iri.cut('/output').add(id: j[:id])} Log
          %td
            &= j[:log]
          %td
            = Zold::Age.new(j[:started])
  %p
    - if offset.positive?
      %a.item{href: iri.over(offset: offset - 25)} Previous
    - if jobs.count >= limit
      %a.item{href: iri.over(offset: offset + 25)} Next
