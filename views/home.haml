%p
  Wallet ID:
  %code
    = user.item.id
  - if user.wallet.exists?
    = ' | '
    Balance:
    = user.wallet.balance

%p
  %a{href: '/pay'} Pay
  - if user.wallet.exists?
    = ' | '
    %a{href: '/push'} Push
  = ' | '
  %a{href: '/pull'} Pull
  = ' | '
  %a{href: '/log'} Log
  = ' | '
  %a{href: '/key'} RSA private key
  = ' | '
  %a{href: '/create', onclick: "return confirm('The wallet will be re-created, the existing RSA private key will be deleted from our database, are you sure?')"} Restart

- if user.wallet.exists?
  %table{style: 'width:100%;display:table;'}
    %thead
      %tr
        %th
          ID
        %th
          Amount
        %th
          Date
        %th
          Beneficiary
        %th
          Details
    %tbody
      - if start
        %tr
          %td{colspan: 5, style: 'text-align:center'}
            = 'Since ' + start.utc.iso8601
      - txns = user.wallet.txns.sort_by(&:date).reverse.select { |t| start.nil? || t.date < start }.take(50)
      - if txns.empty?
        %tr
          %td{colspan: 5, style: 'text-align:center'}
            No transactions in the wallet as of yet.
      - else
        - txns.each do |t|
          - @last = t.date
          %tr
            %td
              - if t.amount.negative?
                = '#' + t.id.to_s
              - else
                = '-'
            %td{style: 'text-align:right'}
              %span{title: t.amount.to_i}
                = t.amount.to_zld
            %td
              = t.date.utc.iso8601
            %td
              %code
                = t.bnf
            %td
              = t.details
        - if @last
          %tr
            %td{colspan: 5, style: 'text-align:center'}
              %a{href: '?start=' + @last.utc.iso8601} Show more
- else
  %p
    Your wallet
    doesn't exist on our server yet,
    %a{href: '/pull'} pull it
    please from the network!
